
#ifndef SELECTION_SORT_ALGORITHM_H
#define SELECTION_SORT_ALGORITHM_H

int switch_lowest_indexes(int*, int, int);
int generate_lowest_index(int*, int, int);

int* switch_array_values(int*, int, int);
int* generate_index_number(int*, int, int);

int* selection_sort_array(int*, int);

#endif
