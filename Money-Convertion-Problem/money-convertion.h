
#ifndef MONEY_CONVERTION_H
#define MONEY_CONVERTION_H

int* generate_values(int);
int generate_amount(int*);

int* add_array_values(int*, int, int);
int remove_amount_money(int, int);

int integer_array_length(int*);
int* generate_integer_array(int);

#endif
