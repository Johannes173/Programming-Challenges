
#ifndef SORTED_SQUARED_ARRAY_H
#define SORTED_SQUARED_ARRAY_H

int* square_integer_array(int*, int);

int* switch_array_numbers(int*, int);
int* bubble_sort_columns(int*, int);
int* bubble_sort_array(int*, int);

int* sort_squared_array(int*, int);

#endif
