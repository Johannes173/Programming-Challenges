
#ifndef FIRST_LAST_INDEX_H
#define FIRST_LAST_INDEX_H

int* switch_array_numbers(int*, int);
int* bubble_sort_columns(int*, int);
int* bubble_sort_array(int*, int);

int generate_integer_array_length(int*);
int* generate_sequence_index(int*, int);

int generate_first_index(int*, int);
int generate_second_index(int*, int);

#endif
